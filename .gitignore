# OS junk only. Do NOT ignore lib/ — the auth_db submodule lives there and
# must stay tracked so the Pages build (checkout submodules: recursive) works.

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor scratch
*~
.claude/
